Brand assets referenced by `app.json` — add before the first EAS build:

- `icon.png` (1024×1024)
- `adaptive-icon.png` (1024×1024, foreground only — background is `#1A1428`)
- `splash-image.png`
- `favicon.png`

Palette: violet gradient `#4C1D95` → `#8B5CF6`, lavender accent `#C4B5FD`, near-black `#1A1428`, off-white `#FAF9FB`.
