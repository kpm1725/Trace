# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/

# Node / Expo — the frontend carries its own .gitignore too
node_modules/
.expo/
dist/
web-build/

# Native projects are generated by `expo prebuild` on the EAS build server.
android/
ios/

# Secrets
.env
.env.*
!.env.example

.DS_Store
